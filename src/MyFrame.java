import javax.swing.*;

public class MyFrame extends JFrame {
    MyFrame(){
        setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        setVisible(true);
        setSize(300,300);
    }
}
